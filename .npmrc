# .npmrc

# Always use auth for installing from registry
always-auth=true

# Point to npm registry
registry=https://registry.npmjs.org/

# Auth token (replace with your environment variable in CI/CD)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
